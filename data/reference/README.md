# Reference Data

Release calendars, WPS/STEO/company-import mappings, and reviewed vendor
workbooks will move here from root `lookup/` and `models/`. The root locations
remain compatibility inputs during the first foundation slice.
